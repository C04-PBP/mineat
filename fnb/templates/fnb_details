<!-- {% extends "base.html" %} -->
<!-- 
{% block meta %}
    <title>{{ fnb.name }} - Food Details</title>
{% endblock meta %}

{% block content %}
<body class="bg-gray-700"></body>
<div class="bg-gray-700 max-w-7xl mx-auto p-4"> -->

    <!-- Back Button -->
    <!-- <div class="flex justify-start mb-4">
        <a href="{% url 'fnb:show_fnb' %}" class="text-indigo-500 hover:text-indigo-700 font-semibold">
            &larr; Back to All Foods
        </a>
    </div> -->

    <!-- Restaurant Name -->
    <!-- <div class="bg-blue-900 shadow-md rounded-lg p-6 mb-4">
        <div class="flex justify-between items-center">
            <h1 class="text-4xl font-bold">{{ fnb.name }}</h1>
            <div class="flex flex-col items-end"> -->
                <!-- <a href="{% url 'restaurant:delete_restaurant' restaurant.pk %}" class="text-red-500 hover:text-red-700 font-semibold mb-1">
                    Delete Food
                </a>
                <a href="{% url 'restaurant:edit_restaurant' restaurant.pk %}" class="text-yellow-500 hover:text-red-700 font-semibold">
                    Edit Food
                </a> -->
            <!-- </div>
        </div>
    </div> -->

    <!-- Food Description -->
    <!-- <div class="bg-blue-900 shadow-md rounded-lg p-6 mb-4">
        <p class="text-gray-700">{{ fnb.address }}</p>
    </div> -->

    <!-- Restaurant District with Link
    <div class="bg-blue-900 shadow-md rounded-lg p-6 mb-4">
        <h2 class="text-2xl font-semibold">District:</h2>
        <p class="text-gray-700">{{ restaurant.location.name }}</p>
        <a href="{% url 'location:location_details' %}?id={{ restaurant.location.id }}" class="text-blue-500 hover:text-blue-700 underline">
            Want to know more about the district?
        </a>
    </div> -->

    <!-- Food and Beverages Section -->
    <!-- <div class="bg-blue-900 shadow-md rounded-lg p-6 mb-4">
        <h2 class="text-2xl font-semibold">Food and Beverages</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 mt-4">
            {% for fnb_item in restaurant.fnb.all %}
                <div class="bg-gray-700 shadow-md rounded-lg p-4">
                    <img src="{{ fnb_item.image }}" alt="{{ fnb_item.name }}" class="w-full h-32 object-cover rounded-md">
                    <h3 class="text-xl font-semibold mt-2">{{ fnb_item.name }}</h3>
                    <p class="text-gray-600 mt-1">{{ fnb_item.description }}</p>
                    <p class="text-indigo-600 font-bold mt-2">Rp {{ fnb_item.price }}</p>
                </div>
            {% empty %}
                <p class="col-span-full text-gray-600">No food and beverages available.</p>
            {% endfor %}
        </div>
    </div> -->
<!-- </div> -->
<!-- {% endblock content %} -->